/**
 * String.prototype.search(
 *  searcher: { [Symbol.search](string: string): number; }
 * ): number;
 * 匹配第一次出现的位置
 * -1 不存在
 * index 索引位置
 * 
 */

namespace Test {
  console.log('hello'.search(/l/))
  
}
