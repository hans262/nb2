/**
 * Set
 * 成员唯一的数组
 * 
 * add(value)
 * has(value)
 * delete(value)
 * size
 * clear()
 * for of
 * 
 */

namespace Test {
  const set = new Set([3, 3])
  set.add(1).add(2).add(1)
  console.log(set)

}